-- PSL HUB WITH KEY SYSTEM 🔑
-- Main entry point with key validation

local ValidKeys = {
    -- PAID KEYS
    ["psl2024premium"] = "paid",
    ["muscle_legends_pro"] = "paid",
    ["vip_access_001"] = "paid",
    ["kev_paid_key"] = "paid",
    ["psl_vip"] = "paid",
    ["premium123"] = "paid",
    
    -- FREE KEYS
    ["psl_free"] = "free",
    ["free_access"] = "free",
    ["try_free"] = "free",
    ["freescript"] = "free",
    
    -- TESTING KEYS
    ["test123"] = "paid",
    ["demo456"] = "free",
}

local function ValidateKey(inputKey)
    if ValidKeys[inputKey] then
        return true, ValidKeys[inputKey]
    end
    return false, nil
end

-- KEY INPUT UI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "KeyInputGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- Main Frame (Mobile Friendly - 310x250)
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 310, 0, 250)
mainFrame.Position = UDim2.new(0.5, -155, 0.5, -125)
mainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

-- Make it draggable
local UserInputService = game:GetService("UserInputService")
local dragging = false
local dragStart
local startPos

mainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        mainFrame.Position = startPos + UDim2.new(0, delta.X, 0, delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

-- Title
local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "Title"
titleLabel.Size = UDim2.new(1, 0, 0, 40)
titleLabel.Position = UDim2.new(0, 0, 0, 0)
titleLabel.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextSize = 18
titleLabel.Font = Enum.Font.GothamBold
titleLabel.Text = "🔑 PSL HUB KEY"
titleLabel.BorderSizePixel = 0
titleLabel.Parent = mainFrame

-- Info Label
local infoLabel = Instance.new("TextLabel")
infoLabel.Name = "InfoLabel"
infoLabel.Size = UDim2.new(1, -20, 0, 40)
infoLabel.Position = UDim2.new(0, 10, 0, 50)
infoLabel.BackgroundTransparency = 1
infoLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
infoLabel.TextSize = 12
infoLabel.Font = Enum.Font.Gotham
infoLabel.Text = "Get key from lootlabs.org\nor use test keys"
infoLabel.TextWrapped = true
infoLabel.Parent = mainFrame

-- Text Box
local textBox = Instance.new("TextBox")
textBox.Name = "KeyInput"
textBox.Size = UDim2.new(1, -20, 0, 35)
textBox.Position = UDim2.new(0, 10, 0, 100)
textBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
textBox.TextColor3 = Color3.fromRGB(255, 255, 255)
textBox.TextSize = 12
textBox.Font = Enum.Font.Gotham
textBox.PlaceholderText = "Paste your key here..."
textBox.Parent = mainFrame

-- Submit Button
local submitButton = Instance.new("TextButton")
submitButton.Name = "SubmitButton"
submitButton.Size = UDim2.new(1, -20, 0, 35)
submitButton.Position = UDim2.new(0, 10, 0, 145)
submitButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
submitButton.TextColor3 = Color3.fromRGB(255, 255, 255)
submitButton.TextSize = 14
submitButton.Font = Enum.Font.GothamBold
submitButton.Text = "✓ SUBMIT KEY"
submitButton.BorderSizePixel = 0
submitButton.Parent = mainFrame

-- Submit Button Click Event
submitButton.MouseButton1Click:Connect(function()
    local enteredKey = textBox.Text:lower()
    
    if enteredKey == "" then
        infoLabel.Text = "❌ Please enter a key!"
        infoLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
        return
    end
    
    local isValid, scriptType = ValidateKey(enteredKey)
    
    if isValid then
        infoLabel.Text = "✅ Key Valid! Loading..."
        infoLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
        
        task.wait(1.5)
        
        -- Load the main script based on type
        if scriptType == "paid" then
            print("✅ Loading PAID PSL Script...")
            LoadPaidScript()
        else
            print("✅ Loading FREE PSL Script...")
            LoadFreeScript()
        end
        
        screenGui:Destroy()
    else
        infoLabel.Text = "❌ Invalid Key! Try again."
        infoLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
        textBox.Text = ""
    end
end)

print("PSL Hub Key System Loaded!")
print("Test keys: test123 (paid), demo456 (free)")

-- ============================================
-- PAID SCRIPT FUNCTION
-- ============================================
function LoadPaidScript()
    local v0 = loadstring(game:HttpGet("https://raw.githubusercontent.com/memejames/elerium-v2-ui-library/refs/heads/main/Library", true))()
    local v1 = v0:AddWindow("PAID PSL HUB 💎", {
        main_color = Color3.fromRGB(200, 0, 0),
        min_size = Vector2.new(310, 450),
        can_resize = false,
        scrollable = true
    })
    
    local v2 = v1:AddTab("INFO ℹ️")
    v2:AddLabel("💲 PAID PSL HUB 💎")
    v2:AddLabel("Owner: Kev ❄️")
    v2:AddLabel("Script: PAID VERSION (FULL FEATURES)")
    local vu3 = v2:AddLabel("🌈 Premium Access 🌈")
    task.spawn(function()
        while true do
            vu3.TextColor3 = Color3.fromHSV(tick() * 0.22 % 1, 0.8, 1)
            task.wait(0.02)
        end
    end)
    v2:AddLabel("═══════════════════════════════════════════")
    v2:AddLabel("FEATURES:")
    v2:AddLabel("✅ Auto Punch")
    v2:AddLabel("✅ Fast Punch")
    v2:AddLabel("✅ Auto Farm")
    v2:AddLabel("✅ Auto Rebirth")
    v2:AddLabel("✅ Kill Players")
    v2:AddLabel("✅ Spy System")
    v2:AddLabel("✅ Anti Knockback")
    v2:AddLabel("✅ Position Lock")
    v2:AddLabel("✅ Size Changer")
    v2:AddLabel("✅ And More!")
    v2:AddLabel("═══════════════════════════════════════════")
    v2:AddButton("Copy Discord", function()
        setclipboard("https://discord.gg/3h9Apg8AQ")
        print("Discord link copied!")
    end)
    
    -- STATS TAB
    local statsTab = v1:AddTab("💪 STATS")
    statsTab:AddLabel("═══════════════════════════════════════════")
    statsTab:AddLabel("⏱️ TIME TRACKING")
    local vu5 = statsTab:AddLabel("Time Spent: 0d 0h 0m 0s")
    local vu6 = os.time()
    local function vu14(p7)
        local v8 = math.floor(p7 / 86400)
        local v9 = p7 % 86400
        local v10 = math.floor(v9 / 3600)
        local v11 = v9 % 3600
        local v12 = math.floor(v11 / 60)
        local v13 = v11 % 60
        return string.format("%dd %dh %dm %ds", v8, v10, v12, v13)
    end
    task.spawn(function()
        while task.wait(1) do
            vu5.Text = "Time Spent: " .. vu14(os.time() - vu6)
        end
    end)
    statsTab:AddLabel("═══════════════════════════════════════════")
    statsTab:AddLabel("📊 REBIRTH STATS")
    local vu15 = 0
    local vu16 = game.Players.LocalPlayer.leaderstats.Rebirths.Value
    local vu17 = statsTab:AddLabel("Rebirths Gained: 0")
    local function vu18()
        vu15 = game.Players.LocalPlayer.leaderstats.Rebirths.Value - vu16
        vu17.Text = "Rebirths Gained: " .. vu15
    end
    task.spawn(function()
        while task.wait(0.5) do
            vu18()
        end
    end)
    
    -- MAIN TAB
    local Main = v1:AddTab("📑 MAIN")
    local v124 = Main:AddFolder("💪 Auto Punch")
    v124:AddSwitch("Auto Punch", function(p125)
        local vu126 = game.Players.LocalPlayer
        local function v132(p127, p128)
            local v129 = vu126:FindFirstChild("Backpack")
            local v130 = vu126.Character or vu126.CharacterAdded:Wait()
            local v131 = v129 and v129:FindFirstChild(p127)
            if v131 then
                v131.Parent = v130
                if p128 then
                    v131.Parent = v129
                end
            end
        end
        if p125 then
            v132("Punch", false)
            local v133 = not vu126.Backpack:FindFirstChild("Punch") and vu126.Character
            if v133 then
                v133 = vu126.Character:FindFirstChild("Punch")
            end
            _G.punchanim = true
            local v134 = vu126
            while _G.punchanim do
                if v133 then
                    if v133.Parent ~= v134.Character then
                        v133.Parent = v134.Character
                    end
                    v133:Activate()
                    task.wait()
                else
                    _G.punchanim = false
                end
            end
        else
            _G.punchanim = false
        end
    end)
    v124:AddSwitch("Fast Punch 👊🏻", function(p135)
        local v136 = game.Players.LocalPlayer
        local v137 = v136.Backpack:FindFirstChild("Punch")
        local v138 = game.Workspace:FindFirstChild(v136.Name)
        local v139
        if v138 then
            v139 = v138:FindFirstChild("Punch")
        else
            v139 = nil
        end
        if p135 then
            if v137 and v137:FindFirstChild("attackTime") then
                v137.attackTime.Value = 0
            elseif v139 and v139:FindFirstChild("attackTime") then
                v139.attackTime.Value = 0
            end
        elseif v137 and v137:FindFirstChild("attackTime") then
            v137.attackTime.Value = 0.35
        elseif v139 and v139:FindFirstChild("attackTime") then
            v139.attackTime.Value = 0.35
        end
    end):Set(false)
    
    Main:AddLabel("═══════════════════════════════════════════")
    Main:AddLabel("🏋️ AUTO FARM")
    local v140 = Main:AddFolder("🏋️ Jungle Machines")
    local vu141 = false
    v140:AddSwitch("Auto Jungle Bench", function(p142)
        vu141 = p142
        if p142 then
            task.spawn(function()
                while vu141 do
                    local v143 = game.Players.LocalPlayer
                    if v143.Character and v143.Character:FindFirstChild("HumanoidRootPart") then
                        v143.Character:SetPrimaryPartCFrame(CFrame.new(-8629.88086, 64.8842468, 1855.03467))
                        game:GetService("ReplicatedStorage").rEvents.machineInteractRemote:InvokeServer("useMachine", workspace.machinesFolder["Jungle Bench"].interactSeat)
                    end
                    task.wait(0.1)
                end
            end)
        end
    end)
    
    Main:AddLabel("═══════════════════════════════════════════")
    Main:AddLabel("💪 AUTO LIFT")
    local v150 = Main:AddFolder("⚙️ Auto Equip")
    local vu151 = false
    v150:AddSwitch("Auto Equip Weight", function(p152)
        vu151 = p152
        if p152 then
            task.spawn(function()
                while vu151 do
                    local v153 = game.Players.LocalPlayer.Backpack:FindFirstChild("Weight")
                    if v153 and game.Players.LocalPlayer.Character then
                        v153.Parent = game.Players.LocalPlayer.Character
                    end
                    task.wait(0.1)
                end
            end)
        end
    end)
    
    -- REBIRTH TAB
    local rebirthTab = v1:AddTab("🔄 REBIRTH")
    rebirthTab:AddLabel("═══════════════════════════════════════════")
    rebirthTab:AddLabel("👑 AUTO KING")
    local vu174 = false
    rebirthTab:AddSwitch("Auto King 👑", function(p175)
        vu174 = p175
        local vu176 = CFrame.new(-8865, 430, -5749)
        local vu177 = game.Players.LocalPlayer
        if p175 then
            local v178 = vu177.Character or vu177.CharacterAdded:Wait()
            if v178 and v178:FindFirstChild("HumanoidRootPart") then
                v178.HumanoidRootPart.CFrame = vu176
            end
            getgenv().kingLockConnection = game:GetService("RunService").Heartbeat:Connect(function()
                local v179 = vu177.Character
                if v179 and v179:FindFirstChild("HumanoidRootPart") then
                    v179.HumanoidRootPart.CFrame = vu176
                end
            end)
        elseif getgenv().kingLockConnection then
            getgenv().kingLockConnection:Disconnect()
            getgenv().kingLockConnection = nil
        end
    end)
    
    rebirthTab:AddLabel("═══════════════════════════════════════════")
    rebirthTab:AddLabel("⚡ REBIRTH FUNCTIONS")
    local vu180 = false
    local vu181 = nil
    rebirthTab:AddTextBox("Target Rebirth", function(p182)
        vu181 = tonumber(p182)
    end, {
        clear = false,
        placeholder = "Enter amount"
    })
    rebirthTab:AddSwitch("Auto Rebirth", function(p183)
        vu180 = p183
        if p183 then
            coroutine.wrap(function()
                while vu180 do
                    local v184 = game.Players.LocalPlayer
                    local v185 = v184:FindFirstChild("leaderstats")
                    if v185 then
                        v185 = v184.leaderstats:FindFirstChild("Rebirths")
                    end
                    if v185 and (vu181 and vu181 <= v185.Value) then
                        print("Target rebirth reached!")
                        vu180 = false
                    else
                        game:GetService("ReplicatedStorage").rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                    end
                    task.wait(0.1)
                end
            end)()
        end
    end)
    
    -- KILLS TAB
    local killsTab = v1:AddTab("💀 KILLS")
    killsTab:AddLabel("═══════════════════════════════════════════")
    killsTab:AddLabel("⚔️ KILL FUNCTIONS")
    killsTab:AddSwitch("Anti Knockback", function(p228)
        if p228 then
            local v229 = game.Players.LocalPlayer.Name
            local v230 = game.Workspace:FindFirstChild(v229):FindFirstChild("HumanoidRootPart")
            local v231 = Instance.new("BodyVelocity")
            v231.MaxForce = Vector3.new(100000, 0, 100000)
            v231.Velocity = Vector3.new(0, 0, 0)
            v231.P = 1250
            v231.Parent = v230
        else
            local v232 = game.Players.LocalPlayer.Name
            local v233 = game.Workspace:FindFirstChild(v232):FindFirstChild("HumanoidRootPart"):FindFirstChild("BodyVelocity")
            if v233 and v233.MaxForce == Vector3.new(100000, 0, 100000) then
                v233:Destroy()
            end
        end
    end)
    
    print("✅ PAID PSL HUB LOADED!")
    print("Press G to toggle GUI")
end

-- ============================================
-- FREE SCRIPT FUNCTION
-- ============================================
function LoadFreeScript()
    local v0 = loadstring(game:HttpGet("https://raw.githubusercontent.com/memejames/elerium-v2-ui-library/refs/heads/main/Library", true))()
    local v1 = v0:AddWindow("FREE PSL HUB 🆓", {
        main_color = Color3.fromRGB(100, 100, 100),
        min_size = Vector2.new(310, 450),
        can_resize = false,
        scrollable = true
    })
    
    local v2 = v1:AddTab("INFO ℹ️")
    v2:AddLabel("🆓 FREE PSL HUB 🆓")
    v2:AddLabel("Owner: Kev ❄️")
    v2:AddLabel("Script: FREE VERSION (LIMITED)")
    local vu3 = v2:AddLabel("⭐ Basic Access ⭐")
    task.spawn(function()
        while true do
            vu3.TextColor3 = Color3.fromHSV(tick() * 0.22 % 1, 0.6, 0.8)
            task.wait(0.02)
        end
    end)
    v2:AddLabel("═══════════════════════════════════════════")
    v2:AddLabel("FREE FEATURES:")
    v2:AddLabel("✅ Auto Punch (Limited)")
    v2:AddLabel("✅ Fast Punch")
    v2:AddLabel("✅ Basic Stats")
    v2:AddLabel("✅ Anti Knockback")
    v2:AddLabel("❌ Auto Farm (PAID)")
    v2:AddLabel("❌ Auto Rebirth (PAID)")
    v2:AddLabel("❌ Kill Players (PAID)")
    v2:AddLabel("❌ Spy System (PAID)")
    v2:AddLabel("═══════════════════════════════════════════")
    v2:AddLabel("Upgrade to PAID for full access!")
    v2:AddButton("Copy Discord", function()
        setclipboard("https://discord.gg/3h9Apg8AQ")
        print("Discord link copied!")
    end)
    
    -- STATS TAB
    local statsTab = v1:AddTab("💪 STATS")
    statsTab:AddLabel("═══════════════════════════════════════════")
    statsTab:AddLabel("⏱️ TIME TRACKING")
    local vu5 = statsTab:AddLabel("Time Spent: 0d 0h 0m 0s")
    local vu6 = os.time()
    local function vu14(p7)
        local v8 = math.floor(p7 / 86400)
        local v9 = p7 % 86400
        local v10 = math.floor(v9 / 3600)
        local v11 = v9 % 3600
        local v12 = math.floor(v11 / 60)
        local v13 = v11 % 60
        return string.format("%dd %dh %dm %ds", v8, v10, v12, v13)
    end
    task.spawn(function()
        while task.wait(1) do
            vu5.Text = "Time Spent: " .. vu14(os.time() - vu6)
        end
    end)
    statsTab:AddLabel("═══════════════════════════════════════════")
    statsTab:AddLabel("📊 BASIC INFO")
    statsTab:AddLabel("Player: " .. game.Players.LocalPlayer.Name)
    local vu16 = game.Players.LocalPlayer.leaderstats.Rebirths.Value
    local vu17 = statsTab:AddLabel("Rebirths: " .. vu16)
    
    -- MAIN TAB
    local Main = v1:AddTab("📑 MAIN")
    local v124 = Main:AddFolder("💪 Auto Punch")
    v124:AddSwitch("Auto Punch", function(p125)
        local vu126 = game.Players.LocalPlayer
        local function v132(p127, p128)
            local v129 = vu126:FindFirstChild("Backpack")
            local v130 = vu126.Character or vu126.CharacterAdded:Wait()
            local v131 = v129 and v129:FindFirstChild(p127)
            if v131 then
                v131.Parent = v130
                if p128 then
                    v131.Parent = v129
                end
            end
        end
        if p125 then
            v132("Punch", false)
            local v133 = not vu126.Backpack:FindFirstChild("Punch") and vu126.Character
            if v133 then
                v133 = vu126.Character:FindFirstChild("Punch")
            end
            _G.punchanim = true
            local v134 = vu126
            while _G.punchanim do
                if v133 then
                    if v133.Parent ~= v134.Character then
                        v133.Parent = v134.Character
                    end
                    v133:Activate()
                    task.wait()
                else
                    _G.punchanim = false
                end
            end
        else
            _G.punchanim = false
        end
    end)
    v124:AddSwitch("Fast Punch 👊🏻", function(p135)
        local v136 = game.Players.LocalPlayer
        local v137 = v136.Backpack:FindFirstChild("Punch")
        local v138 = game.Workspace:FindFirstChild(v136.Name)
        local v139
        if v138 then
            v139 = v138:FindFirstChild("Punch")
        else
            v139 = nil
        end
        if p135 then
            if v137 and v137:FindFirstChild("attackTime") then
                v137.attackTime.Value = 0
            elseif v139 and v139:FindFirstChild("attackTime") then
                v139.attackTime.Value = 0
            end
        elseif v137 and v137:FindFirstChild("attackTime") then
            v137.attackTime.Value = 0.35
        elseif v139 and v139:FindFirstChild("attackTime") then
            v139.attackTime.Value = 0.35
        end
    end):Set(false)
    
    Main:AddLabel("═══════════════════════════════════════════")
    Main:AddLabel("⚠️ PAID FEATURES LOCKED")
    Main:AddLabel("❌ Auto Farm (Upgrade for this)")
    Main:AddLabel("❌ Auto Equip (Upgrade for this)")
    
    -- KILLS TAB
    local killsTab = v1:AddTab("💀 COMBAT")
    killsTab:AddLabel("═══════════════════════════════════════════")
    killsTab:AddLabel("⚔️ BASIC COMBAT")
    killsTab:AddSwitch("Anti Knockback", function(p228)
        if p228 then
            local v229 = game.Players.LocalPlayer.Name
            local v230 = game.Workspace:FindFirstChild(v229):FindFirstChild("HumanoidRootPart")
            local v231 = Instance.new("BodyVelocity")
            v231.MaxForce = Vector3.new(100000, 0, 100000)
            v231.Velocity = Vector3.new(0, 0, 0)
            v231.P = 1250
            v231.Parent = v230
        else
            local v232 = game.Players.LocalPlayer.Name
            local v233 = game.Workspace:FindFirstChild(v232):FindFirstChild("HumanoidRootPart"):FindFirstChild("BodyVelocity")
            if v233 and v233.MaxForce == Vector3.new(100000, 0, 100000) then
                v233:Destroy()
            end
        end
    end)
    
    killsTab:AddLabel("═══════════════════════════════════════════")
    killsTab:AddLabel("⭐ Upgrade to PAID for:")
    killsTab:AddLabel("✅ Kill All Players")
    killsTab:AddLabel("✅ Auto Kill Player")
    killsTab:AddLabel("✅ Spy Player")
    killsTab:AddLabel("✅ Full Combat System")
    
    print("✅ FREE PSL HUB LOADED!")
    print("⭐ Upgrade to PAID for full features!")
    print("Press G to toggle GUI")
end
